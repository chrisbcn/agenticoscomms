
  # Discovery feed screens

  This is a code bundle for Discovery feed screens. The original project is available at https://www.figma.com/design/RHmnLJ724KGmQUfp1ww6GG/Discovery-feed-screens.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  